const fs=require('fs');const path=require('path');const dist=path.join(process.cwd(),'dist');const index=path.join(dist,'index.html');if(!fs.existsSync(index)){console.error('dist/index.html não encontrado.');process.exit(1)}fs.copyFileSync(index,path.join(dist,'404.html'));for(const f of ['_redirects','_headers']){const p=path.join(dist,f);if(fs.existsSync(p))fs.rmSync(p,{force:true})}console.log('Cloudflare SPA fallback OK.');
